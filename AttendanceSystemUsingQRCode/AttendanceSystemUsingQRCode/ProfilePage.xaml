<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             x:Class="AttendanceSystemeUsingQRCode.ProfilePage"
             ControlTemplate="{StaticResource MasterPage }" 
             BackgroundColor="White">

    <ContentPage.Content>
        <ScrollView>  
        <Grid x:Name="mainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="150" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                 
                  
                    <Button Margin="30" ImageSource="s.png"  Clicked="Button_Clicked" Background="#00ffffff"/>
                 
                </Grid>
            <Grid Grid.Row="1">
                  <StackLayout Spacing="10">
                        <Button ImageSource="teacher.png" Margin="40" FontAttributes="Bold" Clicked="teacher_Clicked" Text=" teacher space " BackgroundColor="LightSeaGreen"  CornerRadius="20"/>
                        <Button Margin="40" ImageSource="student.png" Clicked="student_Clicked" Text=" student  space " FontAttributes="Bold" BackgroundColor="LightSeaGreen"  CornerRadius="20"/>

                    </StackLayout>
                </Grid>
        </Grid>
      </ScrollView>
    </ContentPage.Content>
</ContentPage>

